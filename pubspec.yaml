name: boutiquescore
description: BoutiqueScore auto-évaluation web (Dart WASM).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  web: ^1.1.1
  country_currency_iso: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
