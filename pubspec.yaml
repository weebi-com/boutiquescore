name: wasm
description: BoutiqueScore auto-évaluation web (Dart WASM) — rails for client-side scoring.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  web: ^1.1.1
  ml_algo:
    path: ../..
  ml_dataframe: ^1.6.0
  ml_preprocessing: ^7.0.2
  country_currency_iso: ^0.1.0

dev_dependencies:
  build_runner: ^2.4.8
  build_web_compilers: ^4.0.9
  lints: ^5.0.0
  test: ^1.25.0
